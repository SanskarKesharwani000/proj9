package com.sanskar;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class Swm9Application {

	public static void main(String[] args) {
		SpringApplication.run(Swm9Application.class, args);
	}

}


// In this example we use UserDTo to show multiple records from the database ie there is no database now so we provide it.